{% extends 'base.html.twig' %}

{% block title %}Edit Lesson / {{ course.title }} / StudyOn{% endblock %}

{% block body %}
    <h1>Edit Lesson</h1>

    {{ include('lesson/_form.html.twig', {'button_label': 'Update'}) }}

    <a class="btn btn-primary text-white" href="{{ path('app_course_show', {'id': course.id}) }}">Back to course</a>

    {{ include('lesson/_delete_form.html.twig') }}
{% endblock %}
