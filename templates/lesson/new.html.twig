{% extends 'base.html.twig' %}

{% block title %}New Lesson / StudyOn{% endblock %}

{% block body %}
    <h1>Create new Lesson</h1>

    {{ include('lesson/_form.html.twig') }}

    <div style="height: 5px;"></div>
    
    <a class="btn btn-primary text-white" href="{{ path('app_course_show', {'id': course_id}) }}">Back to course</a>
{% endblock %}
