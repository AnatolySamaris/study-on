{% extends 'base.html.twig' %}

{% block title %} {{ lesson.title }} / {{ course.title }} / StudyOn {% endblock %}

{% block body %}
    <h1>{{ lesson.title }}</h1>

    <a class="btn btn-primary text-white" href="{{ path('app_course_show', {'id': course.id}) }}">Back to course</a>

    <a class="btn btn-success text-white" href="{{ path('app_lesson_edit', {'id': lesson.id}) }}">Edit</a>

    {{ include('lesson/_delete_form.html.twig') }}

    <hr style="border: 0; height: 2px; background-color: black;">

    <h3>Content</h3>

    <p>{{ lesson.content }}</p>

{% endblock %}
